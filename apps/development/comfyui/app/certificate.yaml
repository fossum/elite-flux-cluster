apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: comfyui-tls
  namespace: development
spec:
  secretName: comfyui-tls
  issuerRef:
    name: thefoss-le-prod
    kind: ClusterIssuer
  dnsNames:
    - comfyui.thefoss.org
