apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gitlab-agent
  namespace: flux-system
spec:
  interval: 10m
  path: apps/gitlab-agent/gitlab-agent/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
